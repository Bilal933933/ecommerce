# Users Feature (Legacy)

**Description:** إدارة المستخدمين القديمة (قيد الإعادة هيكلة). بعض الدوال قد انتقلت إلى `auth/` و `profile/`.

## Key Components & Functions

| Element | Description |
|---------|-------------|
| `useLogin` | Legacy mutation (قديم) |
| `store.ts` | Legacy user store (قديم) |
| `queries.ts` | Legacy user queries (قديم) |
| `api.ts` | Legacy API calls (قديم) |

## Dependencies & Context
- هذا الموديول في طور الإعادة هيكلة. يفضل استخدام `features/auth/` و `features/profile/`.
